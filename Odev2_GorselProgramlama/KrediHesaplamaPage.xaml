<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Odev2_GorselProgramlama.KrediHesaplamaPage"
             Title="Kredi Hesaplama 💰"
             BackgroundColor="#f0f0f5">

    <ContentPage.Resources>
        <Style TargetType="Label">
            <Setter Property="TextColor" Value="#333333" />
            <Setter Property="FontSize" Value="16" />
        </Style>

        <Style TargetType="Entry">
            <Setter Property="Margin" Value="0" />
            <Setter Property="HeightRequest" Value="48" />
            <Setter Property="PlaceholderColor" Value="#aaaaaa" />
        </Style>

        <Style TargetType="Picker">
            <Setter Property="Margin" Value="0" />
            <Setter Property="HeightRequest" Value="48" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="BackgroundColor" Value="#007bff" />
            <Setter Property="TextColor" Value="White" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="CornerRadius" Value="10" />
            <Setter Property="HeightRequest" Value="55" />
            <Setter Property="Margin" Value="0,15,0,0" />
        </Style>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="20">

            <Label Text="Kredi Türü Seçin:" FontAttributes="Bold"/>
            <Frame Padding="5" CornerRadius="10" BorderColor="#007bff" BackgroundColor="White" HasShadow="False">
                <Picker x:Name="KrediTuruPicker" Title="Kredi Türü Seçin">
                    <Picker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>İhtiyaç Kredisi</x:String>
                            <x:String>Taşıt Kredisi</x:String>
                            <x:String>Konut Kredisi</x:String>
                        </x:Array>
                    </Picker.ItemsSource>
                </Picker>
            </Frame>

            <Label Text="Kredi Tutarı (TL):" FontAttributes="Bold"/>
            <Frame Padding="0" CornerRadius="10" BorderColor="#dddddd" BackgroundColor="White" HasShadow="False">
                <Entry x:Name="TutarEntry"
                       Placeholder="Örn: 100000"
                       Keyboard="Numeric" />
            </Frame>

            <Label Text="Yıllık Faiz Oranı (%):" FontAttributes="Bold"/>
            <Frame Padding="0" CornerRadius="10" BorderColor="#dddddd" BackgroundColor="White" HasShadow="False">
                <Entry x:Name="FaizEntry"
                       Placeholder="Örn: 1.39"
                       Keyboard="Numeric" />
            </Frame>

            <Label Text="Vade (Ay):" FontAttributes="Bold"/>
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10">
                <Slider x:Name="VadeSlider"
                        Minimum="1"
                        Maximum="120"
                        Value="12"
                        MinimumTrackColor="#007bff"
                        MaximumTrackColor="#bbbbbb"
                        ThumbColor="#007bff"
                        ValueChanged="VadeSlider_ValueChanged"
                        Grid.Column="0" />
                <Label x:Name="VadeLabel"
                       Text="12 Ay"
                       VerticalOptions="Center"
                       FontSize="18"
                       FontAttributes="Bold"
                       TextColor="#007bff"
                       Grid.Column="1" />
            </Grid>

            <Button x:Name="HesaplaButton"
                    Text="HESAPLA"
                    Clicked="HesaplaButton_Clicked" />

            <Frame x:Name="SonucLayout" 
                   IsVisible="False" 
                   Margin="0,20,0,0"
                   Padding="20"
                   CornerRadius="15"
                   BackgroundColor="White"
                   BorderColor="#007bff"
                   HasShadow="True">
                <VerticalStackLayout Spacing="10">
                    <Label Text="HESAPLAMA SONUÇLARI" 
                           FontSize="20" 
                           FontAttributes="Bold" 
                           TextColor="#007bff" 
                           HorizontalTextAlignment="Center" 
                           Margin="0,0,0,5"/>
                    <BoxView HeightRequest="1" Color="#dddddd"/>

                    <Label x:Name="AylikTaksitLabel" FontSize="20" FontAttributes="Bold" TextColor="#388e3c" />
                    <BoxView HeightRequest="1" Color="#eeeeee"/>

                    <Label x:Name="ToplamOdemeLabel" FontSize="16" />
                    <Label x:Name="ToplamFaizLabel" FontSize="16" />
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>